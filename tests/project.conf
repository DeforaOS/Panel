targets=applets,applets2,htmllint.log,tests.log,user,wpa_supplicant,xmllint.log
cppflags_force=-I ../include
cflags_force=`pkg-config --cflags libDesktop`
cflags=-W -Wall -g -O2 -pedantic -fPIE -D_FORTIFY_SOURCE=2 -fstack-protector
ldflags_force=`pkg-config --libs libDesktop`
ldflags=-pie -Wl,-z,relro -Wl,-z,now
dist=Makefile,embedded.sh,htmllint.sh,tests.sh,xmllint.sh

[applets]
type=binary
sources=applets.c

[applets2]
type=binary
ldflags=-L../src -L$(OBJDIR). -Wl,-rpath,$(PREFIX)/lib -lPanel
sources=applets2.c

[htmllint.log]
type=script
script=./htmllint.sh
depends=htmllint.sh

[tests.log]
type=script
script=./tests.sh
depends=$(OBJDIR)applets$(EXEEXT),$(OBJDIR)applets2$(EXEEXT),tests.sh,$(OBJDIR)user$(EXEEXT),$(OBJDIR)wpa_supplicant$(EXEEXT)

[user]
type=binary
sources=user.c

[user.c]
depends=../src/applets/user.c

[wpa_supplicant]
type=binary
ldflags=-L../src -L$(OBJDIR). -Wl,-rpath,$(PREFIX)/lib -lPanel
sources=wpa_supplicant.c

[wpa_supplicant.c]
depends=../src/applets/wpa_supplicant.c

[xmllint.log]
type=script
script=./xmllint.sh
depends=xmllint.sh
